import React from 'react';
import ProgressScreen from './ProgressScreen';

/** Kids’ mission progress, badges, and certificates — separate from website DFA. */
const JourneyScreen: React.FC = () => <ProgressScreen />;

export default JourneyScreen;
